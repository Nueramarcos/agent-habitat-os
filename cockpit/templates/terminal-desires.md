# Terminal Desires — Agent Habitat workstation

Agents: **read this file at the start of every session.** Implement unchecked items when reasonable.

## Philosophy

- This machine is a **workshop** for agent-native engineering.
- Prefer **doing** over explaining. Run commands, ship changes.
- Secrets live in `~/.config/cockpit/secrets.env` only.

## Active desires

- [ ] Run `habitat verify` after any provisioning change
- [ ] Configure `~/issue-agent/repos.yaml` with your GitHub repo
- [ ] Run `grok login` or set `XAI_API_KEY` for cloud lane
- [ ] Run `gh auth login` for autonomous PRs

## Completed log

- (agents append dated one-liners here)