name: minimal
description: Local Ollama only — no cloud dependency. Air-gap friendly.

cloud:
  enabled: false

local:
  enabled: true
  pull_models:
    - qwen2.5-coder:7b
    - qwen2.5-coder:1.5b

grok:
  install: false

issue_agent:
  enabled: true
  model: ollama/qwen2.5-coder:7b
  triage_model: qwen2.5-coder:1.5b