# agent-habitat-demo — agent rules

Small Python calculator module with **intentional test failures**. Your job: fix `habitat/calc.py` only.

## Constraints

- Do not change tests unless the test is objectively wrong
- Keep functions pure and minimal
- Run `python3 -m pytest -q` before finishing

## Known bugs (for humans; agents should discover via tests)

Tests in `tests/test_calc.py` document expected behavior.